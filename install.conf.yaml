- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.bash_aliases: bash_aliases
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.condarc: condarc
    ~/.jupyter/custom/custom: jupyter/custom.js
    ~/.jupyter/nbconfig/notebook.json: jupyter/notebook.json
    ~/.jupyter/jupyter_notebook_config.py: jupyter/jupyter_notebook_config.py
    ~/.screenrc:
    ~/scripts/dev-lr: scripts/dev-lr
    ~/scripts/dev-prf: scripts/dev-prf
    ~/.tmux.conf: tmux.conf
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- shell:
  - git update-submodules

